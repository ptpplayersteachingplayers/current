# Booking a package, from the database's side

Generated by `./trace-booking.sh` against PostgreSQL 16. Every number is read
back out of the database after the step above it — this is a transcript, not
an illustration. Re-run it and the ids change; nothing else does.

```

Before — the group Tayo wants to join
    status                             forming
    paid players                       3 of 4 needed to start
    sessions scheduled                 16

1. Simi taps Book — begin_checkout()
    price the server decided           $560 — read from system_settings, never sent by the browser
    hold created                       1 · expires 22:41:50
    the place is occupied              3 paid + 1 held
    bookings so far                    0

2. She taps Book again — the same key
    checkouts                          1 — the unique index on (household, key) held
    holds                              1

3. Stripe takes the money and calls the webhook
    claim_webhook_event                t — first delivery
    settle_checkout                    {"kind": "group_package", "settled": true, "intent_id": "31224eac-986b-41d7-9e4b-f360aad1ea39", "payment_id": "773c28cb-b3b6-480a-9645-6351569c3fc2", "sessions_booked": 16}

4. What that one call did
    payment                            Season package · $560 · succeeded
    package                            16 sessions · expires 2026-10-25
    credits issued                     16
    credits spent on bookings          16
    bookings                           16
    enrolled                           active · paid=true
    hold released                      0
    GROUP STATUS                       confirmed — the fourth paid family started it
    audit entries                      31

5. Stripe delivers the same event again
    claim_webhook_event                f — refused, already handled
    settle_checkout, forced            {"settled": true, "intent_id": "31224eac-986b-41d7-9e4b-f360aad1ea39", "idempotent": true}
    payments                           1 — still one
    bookings                           16 — still sixteen

6. Tayo cannot make week three
    cancel_booking                     {"left_group": false, "refund_due": true, "hours_notice": 1265.6, "makeup_issued": false, "makeup_credit_id": null}
    credit returned                    1
    still enrolled                     active — one session is not the season
    group unchanged                    confirmed — the other five families are unaffected

7. Another family tries to check out for Tayo
    begin_checkout as Mai              ERROR:  That does not belong to your household

```
