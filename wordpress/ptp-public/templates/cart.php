<?php
/**
 * cart view.
 *
 * @var array<string, mixed> $data
 *
 * Scaffolded during the rebuild. Markup is ported from the corresponding old
 * template listed in MIGRATION.md; the data array is already wired, so porting
 * is a matter of moving markup in and deleting the queries it used to run.
 */

if (!defined('ABSPATH')) {
    exit;
}
?>
<div class="ptp ptp-container">
    <h1 class="ptp-h1"><?php esc_html_e('cart', 'ptp'); ?></h1>
    <p class="ptp-empty"><?php esc_html_e('This page is being rebuilt.', 'ptp'); ?></p>
</div>
